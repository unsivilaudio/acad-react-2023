import Container from '@/components/ui/Container';

const UserProfile = () => {
    return (
        <Container>
            <h2 className='text-2xl'>My User Profile</h2>
        </Container>
    );
};

export default UserProfile;
